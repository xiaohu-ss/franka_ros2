# Reset the spine from a fault state (Fault -> SwitchedOff).
---
string state    # Spine state after the reset
bool success    # True if the fault was cleared
string message  # Human-readable status or error description
