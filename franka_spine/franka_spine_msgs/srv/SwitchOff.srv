# Switch the spine off (transition to SwitchedOff, disabling motion).
---
string state    # Spine state after switching off
bool success    # True if the transition succeeded
string message  # Human-readable status or error description
