# Immediately halt any ongoing spine motion (state -> SwitchedOn).
---
string state    # Spine state after the halt
bool success    # True if the halt was executed
string message  # Human-readable status or error description
